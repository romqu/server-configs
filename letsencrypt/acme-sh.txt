#sudo mkdir -p /srv/http/domain/letsencrypt/.well-known/acme-challenge
#sudo chmod -R 0555 /srv/http/domain/letsencrypt/.well-known/acme-challenge/

sudo curl https://get.acme.sh | sh

acme.sh --issue --force -d www.kontorhaus-schlachte.de -w /srv/http/domain/kontorhaus-schlachte.de/ --nginx --debug --keylength ec-384
acme.sh --issue --force -d kontorhaus-schlachte.de -w /srv/http/domain/kontorhaus-schlachte.de/ --nginx --debug --keylength ec-384


mkdir /etc/ssl/private/kontorhaus-schlachte.de
mkdir /etc/ssl/private/www.kontorhaus-schlachte.de

acme.sh --install-cert --ecc --debug -d kontorhaus-schlachte.de \
--cert-file      /etc/ssl/private/kontorhaus-schlachte.de/kontorhaus-schlachte.de.cert.pem  \
--key-file       /etc/ssl/private/kontorhaus-schlachte.de/kontorhaus-schlachte.de.key.pem  \
--fullchain-file /etc/ssl/private/kontorhaus-schlachte.de/fullchain.pem \
--reloadcmd     "systemctl reload nginx.service"

acme.sh --install-cert --ecc --debug -d www.kontorhaus-schlachte.de \
--cert-file      /etc/ssl/private/www.kontorhaus-schlachte.de/www.kontorhaus-schlachte.de.cert.pem  \
--key-file       /etc/ssl/private/www.kontorhaus-schlachte.de/www.kontorhaus-schlachte.de.key.pem  \
--fullchain-file /etc/ssl/private/www.kontorhaus-schlachte.de/fullchain.pem \
--reloadcmd     "systemctl reload nginx.service"
